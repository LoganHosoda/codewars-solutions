// Solution using a funciton declaration
function nameShuffler(str) {
  return str.split(' ').reverse().join(' ')
}

// Reformatted to be one line
const nameShuffler = str => str.split(' ').reverse().join(' ')
